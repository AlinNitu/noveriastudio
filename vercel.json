{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/client",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/api/health", "dest": "/api/health" },
    { "src": "/.*", "dest": "/api" }
  ]
}
